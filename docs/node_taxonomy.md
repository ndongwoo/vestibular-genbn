# Node taxonomy

## Raw observation nodes

Patient-level observations such as positional triggers, nystagmus findings, audiometry, and
safety flags.

## Finding patterns

Derived clinical constructs evaluated from raw observations.

## Disease nodes

Each disease node is evaluated independently as a binary posterior.

## Audit/action outputs

Criteria support labels and safety/action flags. These are not posterior evidence.
